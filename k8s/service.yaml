apiVersion: v1
kind: Service
metadata:
  name: disp-time-service
spec:
  type: NodePort
  selector:
    app: disp-time-web
  ports:
    - protocol: TCP
      port: 80        # External port (Ingress talks to this)
      targetPort: 19563 # Container port
